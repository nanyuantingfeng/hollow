#!/usr/bin/env node --max_old_space_size=2048 

var yargs = require('yargs')

yargs.commandDir('hollow')
  .version(require('../package').version, '-v, --version')
  .demand(1)
  .help()
  .locale('en')
  .showHelpOnFail(true, 'Specify --help for available options')
  .argv
 
